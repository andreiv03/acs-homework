Pentru primele 2 task-uri, citesc matricea de pixeli cu ajutorul careia voi construi recursiv arborele cerut.
La primul task, am o functie pentru fiecare cerinta.
La al doilea task, parcurg arborele pe nivel utilizand o coada pentru a realiza fisierul de compresie.
Pentru ultimul task, construiesc arborele utilizand o coada, pe care, mai apoi, o sa il decompresez recursiv pentru a obtine matricea de pixeli.